# Super 6 Web v0.9

Responsive Super 6 football prediction web app foundation.

This version preserves the approved mobile/desktop prototype and prepares it for the user's existing GitHub + Cloudflare Pages + shared Supabase setup. Super 6 database objects are isolated in the `super6` schema.

See `SETUP.md` for the build path.
