SUPER 6 v0.29.1 — WEBSITE UPDATE ONLY

No SQL required.

Upload the files in this package to the GitHub repository root and replace matching files.
Cloudflare Pages should redeploy automatically.

Chumpions group-stage pairing is now automatic. The old manual Player 1 / Player 2 controls are removed from the website.

If the current Chumpions round was already ticked before the database rotation stages were installed, go to Admin > Round and press Save once after this deployment.
