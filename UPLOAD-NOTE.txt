Super 6 v0.14

Adds an Admin-only bulk roster setup button. It calls the deployed super6-admin-users Edge Function action bulk_create_pending. Existing accounts are skipped. New staged players are created without a PIN and cannot log in until an admin sets one.
