// Super 6 runtime configuration.
// Safe to commit while values are blank. A Supabase publishable key is intended for browser use;
// never put a Supabase secret/service-role key in this file.
window.SUPER6_CONFIG = {
  DEMO_MODE: true,
  SUPABASE_URL: '',
  SUPABASE_PUBLISHABLE_KEY: '',
  SUPABASE_SCHEMA: 'super6'
};
