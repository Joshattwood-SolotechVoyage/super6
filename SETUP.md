# Super 6 v0.29.1 setup

Database: v0.29 automatic Chumpions rotation stages 1-3 are already installed.

No new SQL is required.

1. Upload the website files from this package to the GitHub repository root.
2. Replace matching files.
3. Wait for Cloudflare Pages to redeploy.
4. Hard-refresh Super 6.
5. If the current round is already ticked as a Chumpions League week, open Admin → Round and press Save once to generate its automatic group fixtures.
